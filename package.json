{
  "name": "n8n-clever-cloud",
  "version": "1.0.0",
  "description": "n8n on clevercloud",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix ./.n8n-runtime n8n@2.26.8 --omit=dev --no-audit --no-fund",
    "start": "bash ./run.sh"
  },
  "engines": {
    "node": "22.x"
  }
}
